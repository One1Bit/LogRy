Ver 1.1
- Open build-in log file (LogBuild.txt) or log of the same format (subdivided Tab, 4 columns) 
- Clear
- Beta version settings 
 How to start? 
 1. Download the Git (https://git-scm.com/downloads) and install it.
 2. Create a folder and open Git Bush (right mouse button->Git Bush here)
 3. enter this command:
 $ git clone https://github.com/One1Bit/LogRy
 $ git config --global user.name "Your Name"
 $ git config --global user.email "your_email@whatever.com"